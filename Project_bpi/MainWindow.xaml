<Window x:Class="Project_bpi.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Project_bpi"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Title="MainWindow" Height="700" Width="900">

    <Window.Resources>
        <Style x:Key="CalendarComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#ced4da"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Border Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                            <Grid>
                                <ToggleButton x:Name="ToggleButton"
                                      Background="Transparent"
                                      BorderBrush="Transparent"
                                      Focusable="false"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"/>
                                <ContentPresenter x:Name="ContentSite"
                                          IsHitTestVisible="False"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                          Margin="4,2"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Left"/>
                                <Popup x:Name="Popup"
                               Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True"
                               Focusable="False"
                               PopupAnimation="Slide">
                                    <Border Background="White"
                                    BorderBrush="#ced4da"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    MaxHeight="150">
                                        <ScrollViewer>
                                            <ItemsPresenter/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#adb5bd"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter Property="BorderBrush" Value="#0167a4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для кнопки "Применить" -->
        <Style x:Key="CalendarApplyButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#0167a4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                        CornerRadius="8"
                        Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="6" Opacity="0.15" ShadowDepth="2" Color="#000000"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#015a8f"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#014d78"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="CustomScrollBar" TargetType="{x:Type ScrollBar}">
            <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="8"/>
            <Setter Property="MinWidth" Value="8"/>
            <Setter Property="Opacity" Value="0.3"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid x:Name="Bg" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                            <Track x:Name="PART_Track" IsDirectionReversed="true" IsEnabled="{TemplateBinding IsMouseOver}">
                                <Track.Thumb>
                                    <Thumb Style="{DynamicResource ScrollBarThumbVertical}"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для Thumb (бегунка) -->
        <Style x:Key="ScrollBarThumbVertical" TargetType="{x:Type Thumb}">
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border x:Name="rectangle"
                            Width="8"
                            Background="#A0A0A0"
                            CornerRadius="4"
                            SnapsToDevicePixels="True"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="rectangle" Property="Background" Value="#707070"/>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="rectangle" Property="Background" Value="#505050"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для ScrollViewer с красивым скроллом -->
        <Style x:Key="BeautifulScrollViewer" TargetType="{x:Type ScrollViewer}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollViewer}">
                        <Grid x:Name="Grid" Background="{TemplateBinding Background}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Rectangle x:Name="Corner" Grid.Row="1" Grid.Column="1" Fill="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"/>

                            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter"
                                                Grid.Row="0"
                                                Grid.Column="0"
                                                Margin="{TemplateBinding Padding}"
                                                CanContentScroll="{TemplateBinding CanContentScroll}"
                                                CanHorizontallyScroll="False"
                                                CanVerticallyScroll="False"
                                                Content="{TemplateBinding Content}"
                                                ContentTemplate="{TemplateBinding ContentTemplate}"/>

                            <ScrollBar x:Name="PART_VerticalScrollBar"
                                   Grid.Row="0"
                                   Grid.Column="1"
                                   AutomationProperties.AutomationId="VerticalScrollBar"
                                   Cursor="Arrow"
                                   Maximum="{TemplateBinding ScrollableHeight}"
                                   Minimum="0"
                                   Style="{StaticResource CustomScrollBar}"
                                   ViewportSize="{TemplateBinding ViewportHeight}"
                                   Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                   Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"/>

                            <ScrollBar x:Name="PART_HorizontalScrollBar"
                                   Grid.Row="1"
                                   Grid.Column="0"
                                   AutomationProperties.AutomationId="HorizontalScrollBar"
                                   Cursor="Arrow"
                                   Maximum="{TemplateBinding ScrollableWidth}"
                                   Minimum="0"
                                   Orientation="Horizontal"
                                   ViewportSize="{TemplateBinding ViewportWidth}"
                                   Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"
                                   Value="{Binding HorizontalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <DrawingImage x:Key="ArrowChevronBlueIcon">
            <DrawingImage.Drawing>
                <GeometryDrawing Brush="#0167a4" Geometry="F1 M 29.6,4.6L 11.6,22.6L 10.2,24L 11.6,25.4L 29.6,43.4L 32.5,40.5L 15.9,24L 32.4,7.4L 29.6,4.6 Z"/>
            </DrawingImage.Drawing>
        </DrawingImage>

        <DrawingImage x:Key="ArrowChevronWhiteIcon">
            <DrawingImage.Drawing>
                <GeometryDrawing Brush="#FFFFFF" Geometry="F1 M 29.6,4.6L 11.6,22.6L 10.2,24L 11.6,25.4L 29.6,43.4L 32.5,40.5L 15.9,24L 32.4,7.4L 29.6,4.6 Z"/>
            </DrawingImage.Drawing>
        </DrawingImage>

        <Style x:Key="ArrowIndicatorStyle" TargetType="Image">
            <Setter Property="Width" Value="11.2"/>
            <Setter Property="Height" Value="11.2"/>
            <Setter Property="Margin" Value="6,0,0,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Stretch" Value="Uniform"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        </Style>

        <!-- Стили остаются без изменений -->
        <Style x:Key="MenuHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#0167a4"/>
            <Setter Property="FontFamily" Value="Raleway"/>
        </Style>

        <Style x:Key="SubMenuStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#0167a4"/>
            <Setter Property="FontFamily" Value="Raleway"/>
        </Style>
        <Style x:Key="SubMenuStyle1" TargetType="TextBlock">
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#8a8a8a"/>
            <Setter Property="FontFamily" Value="Raleway"/>
        </Style>

        <Style x:Key="MenuItemStyle" TargetType="Border">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style x:Key="ActiveMainItemStyle" TargetType="Border" BasedOn="{StaticResource MenuItemStyle}">
            <Setter Property="CornerRadius" Value="0,15,15,0"/>
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#0167a4" Offset="0"/>
                        <GradientStop Color="#005e9d" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="TextElement.Foreground" Value="White"/>
        </Style>

        <Style x:Key="ActiveSubItemStyle" TargetType="Border" BasedOn="{StaticResource MenuItemStyle}">
            <Setter Property="CornerRadius" Value="0,15,15,0"/>
            <Setter Property="TextElement.Foreground" Value="White"/>
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#5394ba" Offset="0"/>
                        <GradientStop Color="#0167a4" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActiveSubItemLevel2Style" TargetType="Border" BasedOn="{StaticResource MenuItemStyle}">
            <Setter Property="CornerRadius" Value="0,15,15,0"/>
            <Setter Property="TextElement.Foreground" Value="White"/>
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#b8d3df" Offset="0"/>
                        <GradientStop Color="#5394ba" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActiveItemStyle" TargetType="Border" BasedOn="{StaticResource MenuItemStyle}">
            <Setter Property="Background" Value="#005e9d"/>
            <Setter Property="BorderBrush" Value="#004777"/>
            <Setter Property="BorderThickness" Value="4,0,0,0"/>
        </Style>
        <Style x:Key="SearchBoxBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="#f8f9fa"/>
            <Setter Property="BorderBrush" Value="#c4c4c4"/>
            <Setter Property="BorderThickness" Value="3"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="2"/>
            <Setter Property="Margin" Value="5,0,5,5"/>
        </Style>
        <Style x:Key="SidebarOutlineButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#0167a4"/>
            <Setter Property="BorderBrush" Value="#d6e2ea"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,2"/>
            <Setter Property="Margin" Value="10,8,10,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="15"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#f0f8ff"/>
                                <Setter Property="Foreground" Value="#004c8c"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#e6f2ff"/>
                                <Setter Property="Foreground" Value="#003d73"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AddTemplateButtonStyle" TargetType="Button" BasedOn="{StaticResource SidebarOutlineButtonStyle}">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Margin" Value="10,6,10,10"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
        <Style x:Key="CalendarNavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#0167a4"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="16">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#e3f2fd"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <!-- Стиль для вложенных подразделов (3-й уровень) -->
        <Style x:Key="MenuItemSubSubStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Foreground" Value="#6c757d"/>
            <Setter Property="FontFamily" Value="Raleway"/>
            <Setter Property="Margin" Value="12,0,0,0"/>
            <!-- Отступ слева -->
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="180"/>
        </Grid.ColumnDefinitions>

        <!-- Левая навигационная панель -->
        <Border Grid.Column="0"
                Background="#f8f9fa"
                BorderBrush="#d8e0e5"
                BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Основное содержимое меню -->
                <ScrollViewer Grid.Row="0"
              Style="{StaticResource BeautifulScrollViewer}"
              VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!-- Логотип -->
                        <StackPanel Margin="10" HorizontalAlignment="Center">
                            <Image Source="/images/logo.jpg"
                                   Width="180"
                                   Height="90"
                                   Stretch="Fill"/>
                        </StackPanel>

                        <Rectangle Height="1" Fill="#d6e2ea" Margin="10,0,10,10"/>

                        <!-- Меню навигации -->
                        <StackPanel>
                            <!-- Поиск -->
                            <Border Style="{StaticResource SearchBoxBorderStyle}">
                                <Grid>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <Image Source="/images/search.jpg" Width="16" Height="16" Margin="5,0"/>
                                        <TextBox x:Name="SearchBox"
                     Width="200"
                     Margin="5,0"
                     Padding="5,2"
                     Background="#f8f9fa"
                     BorderThickness="0"
                     Foreground="#495057"
                     FontSize="12"
                     TextChanged="SearchBox_TextChanged"/>
                                    </StackPanel>
                                    <TextBlock x:Name="SearchPlaceholder"
                   Text="Поиск"
                   Foreground="#8a8a8a"
                   Margin="30,2,0,0"
                   VerticalAlignment="Center"
                   IsHitTestVisible="False"/>
                                </Grid>
                            </Border>

                            <StackPanel x:Name="DynamicTemplatesPanel"/>
                            <Button x:Name="AddTemplateButton"
                                    Content="Добавить шаблон"
                                    Click="AddTemplateButton_Click"
                                    Style="{StaticResource AddTemplateButtonStyle}"/>
                            <Border x:Name="Item_Archive" MouseLeftButtonDown="Item_Archive_Click" Tag="main" Style="{StaticResource MenuItemStyle}">
                                <TextBlock Text="Архив" Style="{StaticResource MenuHeaderStyle}"/>
                            </Border>
                            <Border x:Name="Item_Templates" MouseLeftButtonDown="MenuItem_Click" Tag="main" Style="{StaticResource MenuItemStyle}">
                                <TextBlock Text="Шаблоны" Style="{StaticResource MenuHeaderStyle}"/>
                            </Border>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
                <!-- Справка - в самом низу -->
                <StackPanel Grid.Row="1" Margin="0,10,0,10">
                    <Rectangle Height="1" Fill="#d6e2ea" Margin="10,0,10,10"/>
                    <TextBlock Width="224" Height="18"><Hyperlink Click="HelpHyperlink_Click" 
                   Foreground="#0167a4"
                   TextDecorations="{x:Null}"><Run Text="Справка"/></Hyperlink></TextBlock>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Основной контент -->
        <Border Grid.Column="1" Background="#e8e8e8">
            <ContentControl x:Name="MainContentControl">
                <ContentControl.Content>
                    <TextBlock Text="Основная область контента"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Foreground="#6c757d"
                       FontSize="16"/>
                </ContentControl.Content>
            </ContentControl>
        </Border>

        <!-- Правая панель с кнопками внизу -->
        <Border Grid.Column="2"
        Background="#f1f3f5"
        BorderBrush="#d6e2ea"
        BorderThickness="1,0,0,0">
            <Grid Cursor="">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Блок с периодом для истории изменений -->
                <Grid x:Name="HistoryPeriodPanel" Visibility="Collapsed">
                <Border Background="White"
        CornerRadius="10"
        Padding="10"
        Margin="10"
        BorderBrush="#d6e2ea"
        BorderThickness="1"
        VerticalAlignment="Top">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="Период" FontWeight="Bold" Foreground="#0167a4"/>

                        <!-- Кликабельная область для выбора периода -->
                        <Button x:Name="PeriodSelector" 
        Click="PeriodSelector_Click"
        Background="Transparent"
        BorderThickness="0"
        Cursor="Hand">
                            <!-- Содержимое вашей кнопки выбора периода -->
                            <StackPanel Orientation="Horizontal">
                                <TextBlock x:Name="DateRangeText" Text="01.01 - 31.12"/>
                                <TextBlock x:Name="YearText" Text="2025" Margin="5,0,0,0"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Border>
                <Popup x:Name="CalendarPopup"
       Placement="Bottom"
       PlacementTarget="{Binding ElementName=PeriodSelector}"
       StaysOpen="False"
       AllowsTransparency="True"
       PopupAnimation="Slide">
                    <Border Background="White"
            BorderBrush="#d6e2ea"
            BorderThickness="1"
            CornerRadius="14"
            Padding="20"
            Margin="0,8,0,0">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="20" Opacity="0.12" ShadowDepth="4" Color="#000000"/>
                        </Border.Effect>
                        <StackPanel>
                            <!-- Заголовок -->
                            <TextBlock Text="Период"
                       FontWeight="Bold"
                       Foreground="#0167a4"
                       FontSize="16"
                       Margin="0,0,0,16"
                       HorizontalAlignment="Center"/>

                            <!-- Навигация по месяцам -->
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,16">
                                <Button Content="‹"
                        Style="{StaticResource CalendarNavButtonStyle}"
                        Click="PreviousMonth_Click"/>
                                <TextBlock x:Name="CurrentMonthYear"
                           Text="Октябрь 2025"
                           FontWeight="SemiBold"
                           FontSize="14"
                           Foreground="#495057"
                           VerticalAlignment="Center"
                           Margin="16,0"/>
                                <Button Content="›"
                        Style="{StaticResource CalendarNavButtonStyle}"
                        Click="NextMonth_Click"/>
                            </StackPanel>

                            <!-- Дни недели -->
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Пн" Grid.Column="0" HorizontalAlignment="Center" FontSize="11" Foreground="#868e96"/>
                                <TextBlock Text="Вт" Grid.Column="1" HorizontalAlignment="Center" FontSize="11" Foreground="#868e96"/>
                                <TextBlock Text="Ср" Grid.Column="2" HorizontalAlignment="Center" FontSize="11" Foreground="#868e96"/>
                                <TextBlock Text="Чт" Grid.Column="3" HorizontalAlignment="Center" FontSize="11" Foreground="#868e96"/>
                                <TextBlock Text="Пт" Grid.Column="4" HorizontalAlignment="Center" FontSize="11" Foreground="#868e96"/>
                                <TextBlock Text="Сб" Grid.Column="5" HorizontalAlignment="Center" FontSize="11" Foreground="#868e96"/>
                                <TextBlock Text="Вс" Grid.Column="6" HorizontalAlignment="Center" FontSize="11" Foreground="#868e96"/>
                            </Grid>

                            <!-- Календарь -->
                            <ItemsControl x:Name="CalendarDays" HorizontalAlignment="Stretch">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Columns="7" Rows="6"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border MinWidth="32" MinHeight="32"
                                Background="{Binding BackgroundColor}"
                                CornerRadius="16"
                                Margin="2"
                                Cursor="Hand"
                                MouseLeftButtonUp="CalendarDay_MouseLeftButtonUp">
                                            <TextBlock Text="{Binding Day}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontSize="12"
                                       Foreground="{Binding TextColor}"
                                       FontWeight="{Binding FontWeight}"/>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <!-- Выбор периода через ComboBox -->
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,16,0,14">
                                <!-- Начало -->
                                <Border Background="#f8f9fa" CornerRadius="8" Padding="12" Margin="0,0,16,0">
                                    <StackPanel>
                                        <TextBlock Text="Период с:"
                                   FontSize="11"
                                   Foreground="#6c757d"
                                   FontWeight="SemiBold"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,8"/>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <ComboBox x:Name="StartDayComboBox" Style="{StaticResource CalendarComboBoxStyle}" Width="42" Margin="0,0,4,0" SelectionChanged="StartDate_SelectionChanged"/>
                                            <ComboBox x:Name="StartMonthComboBox" Style="{StaticResource CalendarComboBoxStyle}" Width="52" Margin="0,0,4,0" SelectionChanged="StartDate_SelectionChanged"/>
                                            <ComboBox x:Name="StartYearComboBox" Style="{StaticResource CalendarComboBoxStyle}" Width="52" SelectionChanged="StartDate_SelectionChanged"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>

                                <!-- Конец -->
                                <Border Background="#f8f9fa" CornerRadius="8" Padding="12">
                                    <StackPanel>
                                        <TextBlock Text="по:"
                                   FontSize="11"
                                   Foreground="#6c757d"
                                   FontWeight="SemiBold"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,8"/>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <ComboBox x:Name="EndDayComboBox" Style="{StaticResource CalendarComboBoxStyle}" Width="42" Margin="0,0,4,0" SelectionChanged="EndDate_SelectionChanged"/>
                                            <ComboBox x:Name="EndMonthComboBox" Style="{StaticResource CalendarComboBoxStyle}" Width="52" Margin="0,0,4,0" SelectionChanged="EndDate_SelectionChanged"/>
                                            <ComboBox x:Name="EndYearComboBox" Style="{StaticResource CalendarComboBoxStyle}" Width="52" SelectionChanged="EndDate_SelectionChanged"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </StackPanel>

                            <!-- Кнопка "Применить" -->
                            <Button Content="Применить"
                    Click="ApplyDateRange_Click"
                    Style="{StaticResource CalendarApplyButtonStyle}"/>
                        </StackPanel>
                    </Border>
                </Popup>
                </Grid>
                <!-- Кнопки - перемещены вниз -->
                <StackPanel Grid.Row="1" Margin="10">
                    <!-- Кнопка "История изменений" -->
                    <Button Click="HistoryButton_Click"
        IsEnabled="True"
        Style="{StaticResource SidebarOutlineButtonStyle}">
                        <TextBlock Text="История&#x0a;изменений" 
               TextAlignment="Center" 
               TextWrapping="Wrap"/>
                    </Button>
                    <!-- Кнопка "Сформировать итоговый отчет" -->
                    <Button Height="45"
            Background="#0167a4" 
            Foreground="White" 
            FontWeight="SemiBold"
            BorderThickness="0"
            Padding="10,2"
            Margin="0,5,0,0"
            Click="GenerateFinalReportButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="15"
                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                        <TextBlock Text="Сформировать&#x0a;итоговый отчет" 
                   TextAlignment="Center" 
                   TextWrapping="Wrap"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
