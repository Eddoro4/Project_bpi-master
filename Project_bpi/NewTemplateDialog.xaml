<Window x:Class="Project_bpi.NewTemplateDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Новый шаблон" Height="180" Width="360" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="Имя шаблона" FontWeight="SemiBold"/>
            <TextBox x:Name="NameTextBox"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Margin="0,0,0,8">
            <TextBlock Text="Год" FontWeight="SemiBold"/>
            <TextBox x:Name="YearTextBox"/>
        </StackPanel>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" Width="80" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="ОК" Width="80" Click="Ok_Click"/>
        </StackPanel>
    </Grid>
</Window>